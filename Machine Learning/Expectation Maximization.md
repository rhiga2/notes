# Latent Variable Models
# Evidence Lower Bound


$$
\begin{aligned}
l(\theta) &= p_\theta(X) = E_{Z \sim q} \left[ p_\theta(X) \right] \\
	&= E_{Z \sim q} \left[ p_\theta(X) \frac{q(Z)}{q(Z)} \right]
\end{aligned}
$$

